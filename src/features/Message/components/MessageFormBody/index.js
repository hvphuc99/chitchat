import React from 'react';
import PropTypes from 'prop-types';

MessageFormBody.propTypes = {
  
};

function MessageFormBody(props) {
  return (
    <div>
      Body
    </div>
  );
}

export default MessageFormBody;